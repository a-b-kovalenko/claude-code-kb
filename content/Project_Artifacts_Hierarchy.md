[⬅️](../Claude_Code_Knowledge_Base.md)

## 📝 TL;DR

Усі артефакти керування агентом живуть у двох місцях: корінь проєкту (`CLAUDE.md`, `.mcp.json`, `.claude/`) та домашня директорія (`~/.claude/`). Проєктні файли — спільні для команди та комітяться в Git; персональні — тільки ваші, на всіх проєктах.

## Структура папки проєкту

```text
project-root/
│
├── CLAUDE.md                        ← головний контракт з агентом
├── .mcp.json                        ← MCP-сервери для команди (комітити)
│
└── .claude/
    ├── settings.json                ← дозволи, env, модель (комітити)
    ├── settings.local.json          ← локальні перевизначення (не комітити)
    │
    ├── rules/                       ← модульні правила (завжди в контексті)
    │   ├── jpa.md
    │   ├── kafka.md
    │   └── security.md
    │
    ├── skills/                      ← скіли: auto-load + /команда
    │   ├── jpa-patterns/
    │   │   └── SKILL.md
    │   └── spring-security/
    │       └── SKILL.md
    │
    ├── agents/                      ← субагенти
    │   ├── explorer.md
    │   ├── test-runner.md
    │   └── reviewer.md
    │
    ├── hooks/                       ← shell-скрипти подій
    │   ├── bash-guard.sh
    │   └── flyway-guard.sh
    │
    └── commands/                    ← старий формат slash-команд (досі працює)
        └── review.md
```

## Призначення кожного артефакту

| Артефакт | Коли читається агентом | Призначення |
| :--- | :--- | :--- |
| `CLAUDE.md` | На початку кожної сесії, завжди | Стек, структура, ключові правила та заборони |
| `.mcp.json` | При старті сесії | Спільні MCP-сервери для команди (project scope) |
| `.claude/rules/*.md` | На початку сесії, завжди | Модульні правила для конкретних тем (JPA, Kafka) |
| `.claude/skills/<name>/SKILL.md` | Автоматично за темою або через `/name` | Глибока технічна експертиза; новий уніфікований формат |
| `.claude/agents/*.md` | При делегуванні задачі субагенту | Роль, system prompt, модель та whitelist інструментів |
| `.claude/hooks/*.sh` | При відповідній події (PreToolUse тощо) | Детерміновані перевірки та автоматизація |
| `.claude/commands/*.md` | При явному виклику `/назва` | Старий flat-file формат slash-команд; досі працює |
| `.claude/settings.json` | При старті сесії | Дозволи інструментів, змінні середовища, модель |
| `.claude/settings.local.json` | При старті сесії | Локальні перевизначення (секрети, особисті налаштування) |

## Проєктний vs персональний рівень

```text
~/.claude/                           ← персональний (усі проєкти)
├── CLAUDE.md                        ← глобальні правила для всіх проєктів
├── settings.json                    ← глобальні дозволи та налаштування
├── skills/                          ← персональні скіли та slash-команди
│   ├── handoff/
│   │   └── SKILL.md
│   └── init-project/
│       └── SKILL.md
└── plugins/                         ← встановлені плагіни з маркетплейсу
```

Якщо один і той самий ключ є в обох місцях — перемагає проєктний рівень. `allow`/`deny`-списки об'єднуються.

## Порядок завантаження (пріоритет)

```text
Enterprise policy  →  ~/.claude/  →  .claude/settings.json  →  .claude/settings.local.json
```

`CLAUDE.md` та `rules/` — завжди в контексті. `skills/` — за потребою (автоматична підвантажка). Хуки та settings.json — незалежно від контексту, спрацьовують за подіями.

## Куди що класти: вирішальні питання

**Правило потрібне щоразу?** → `CLAUDE.md` або `.claude/rules/`  
**Правило потрібне тільки для конкретної теми або треба slash-команда?** → `.claude/skills/<name>/SKILL.md`  
**Потрібна умовна логіка або зупинка дії?** → `.claude/hooks/`  
**Потрібен статичний allow/deny без логіки?** → `.claude/settings.json`  
**Простий шаблон промпту (legacy)?** → `.claude/commands/*.md`  
**Треба делегувати підзадачу?** → `.claude/agents/`  
**Потрібен прямий доступ до інфраструктури (БД, API)?** → `.mcp.json`  
**Треба поділитися skills/agents з командою або між проєктами?** → плагін у `~/.claude/skills/<name>/` з маніфестом `.claude-plugin/plugin.json`

## Зв'язок з іншими нотатками

- Що писати в `CLAUDE.md` та як структурувати: [📄 Написання CLAUDE.md](CLAUDE_md_Writing_Guide.md).
- Ієрархія завантаження та цикл компіляції знань: [🧠 Ієрархія пам'яті та синтез](Memory_Hierarchy_and_Synthesis.md).
- Конфігурація `settings.json`: [⚙️ Конфігурація settings.json](Settings_JSON_Guide.md).
- Анатомія скіл-файлу: [✍️ Розробка власного скіла](Skill_Development_Guide.md).
- Анатомія хука: [🔧 Розробка власного хука](Hook_Development_Guide.md).
- Анатомія субагента: [👥 Архітектура субагентів](Subagents_Architecture.md).
- Анатомія slash-команди: [⚡ Кастомні Slash-команди](Custom_Slash_Commands.md).
- Реєстрація та розробка MCP-серверів: [🔌 Розробка MCP-сервера](MCP_Server_Development.md).
- Плагіни — шеринг та маркетплейси: [📦 Плагіни](Plugins.md).
